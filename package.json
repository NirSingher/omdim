{
  "name": "standup-bot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@slack/web-api": "^7.0.0",
    "pg": "^8.11.0",
    "yaml": "^2.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/pg": "^8.10.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}
