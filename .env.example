# Vercel / Netlify local development
# Copy to .env: cp .env.example .env
# For Cloudflare Workers, use .dev.vars.example instead
#
# IMPORTANT: Use a SEPARATE Slack app for local development!
# - Create a second Slack app (e.g., "Omdim Dev")
# - Point its Request URL to your local tunnel (ngrok, cloudflared, etc.)
# - Production credentials go in your hosting provider's environment settings

# Slack App Credentials (DEV app)
SLACK_BOT_TOKEN=xoxb-your-DEV-bot-token
SLACK_SIGNING_SECRET=your-DEV-signing-secret

# Database
DATABASE_URL=postgres://user:password@host:5432/database

# Cron Security (for external cron services like cron-job.org)
CRON_SECRET=your-random-secret-string
